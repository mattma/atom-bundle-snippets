AtomBundleSnippetsView = require '../lib/atom-bundle-snippets-view'
{WorkspaceView} = require 'atom'

describe "AtomBundleSnippetsView", ->
  it "has one valid test", ->
    expect("life").toBe "easy"
